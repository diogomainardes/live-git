console.log("Meu primeiro commit")

const somar = (x, y) => {
    return x + y;
}

const exponencial = function (x, y) {
    return x ** y
}

console.log('A soma é' + somar(10, 20));

console.log(`O exponencial é ${exponencial(10, 3)}`);

/* Pelo GitHub */
